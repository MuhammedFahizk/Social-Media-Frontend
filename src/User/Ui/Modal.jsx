import React, { useState } from 'react';
import { Button, Modal } from 'antd';
const ModalComponent = ({showModal, setIsModalOpen, isModalOpen}) => {


  return (
    <>
      {/* <Button type="primary" onClick={showModal}>
        Open Modal
      </Button> */}
      
    </>
  );
};
export default ModalComponent;